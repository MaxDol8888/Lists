#Soroka
import random
a=[random.randint(0,100) for i in range(12)]
print(*a)
b=a.copy()
for i in range(1,len(a),1):
    if(i==len(a)-1):
        a[0]=b[len(a)-1]
        a[len(a)-1]=b[i-1]
    else:
        a[i]=b[i-1]
print(*a)
