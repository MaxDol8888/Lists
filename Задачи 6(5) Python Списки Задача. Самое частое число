#Soroka
import random
a=[random.randint(0,10) for i in range(6)]
print(*a)
sch=0
naib=0
for i in a:
    if(a.count(i)>sch):
        sch=a.count(i)
        naib=i
    else:
        continue
print(naib)


#Kondrachuk
a=[int(s) for s in input().split()]
b=0
c=0
for i in range(len(a)):
    if a.count(a[b])<a.count(a[i]):
        b=i
        c=a[i]
print(c)


#Kamenev
s = list(map(int, input().split()))
max = s[0]
max_count = s.count(max)
for el in s:
 if s.count(el)>max_count:
    max = el
 max_count = s.count(el)
print(max)

